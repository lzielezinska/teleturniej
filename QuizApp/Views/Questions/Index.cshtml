@model QuizApp.Models.QuestionAnswersViewModel

@{ 
    var nextQuestionID = Model.Question.Id + 1;
}

<div class="card p-3">
    <div class="card-title mb-3 text-center">
        @Model.Question.Content
    </div>
    <div class="d-flex flex-wrap">
        @foreach (var answer in Model.Answers)
        {
            <div class="col-sm-6 col-12">
                <div class="m-2 card answer-card mx-auto" value="@Html.Raw(answer.IsCorrect)">
                    <div class="row h-100 w-100 m-0">
                        <div class="col-12 my-auto text-center">
                            @answer.Content
                        </div>
                     </div>
                </div>
            </div>
        }
    </div>
    <div class="ml-auto p-2">
        <a class="btn btn-green" asp-action="Index" asp-route-id="@nextQuestionID">Następne</a>
    </div>
</div>